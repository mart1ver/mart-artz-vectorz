"""LuxCore DMX Engine — moteur Python (portage moderngl).

Package du portage. Sous-modules :
  constants  — mapping DMX, LUT blend, formes (fidèle à z_fixture_definition.pde)
  dmx        — décodage des octets DMX en état exploitable (base + spots)
  artnet     — réception ArtNet UDP multi-univers (thread)
"""
